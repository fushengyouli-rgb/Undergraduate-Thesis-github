# 胎心信号异常检测轻量化CNN-GRU模型训练流程

## 1. 流程图（Mermaid语法）

```mermaid
flowchart TB
    subgraph 输入["输入层"]
        A1["FHR 胎心率信号<br/>256点 × 1通道"]
        A2["UC 宫缩信号<br/>256点 × 1通道"]
    end

    subgraph 预处理["数据预处理"]
        B1["滑动窗口分割<br/>步长64秒"]
        B2["归一化处理"]
    end

    subgraph 手工特征["手工特征提取"]
        C1["基线变异"]
        C2["短期变异性"]
        C3["长期变异性"]
        C4["加速事件比例"]
        C5["减速事件比例"]
        C["5维手工特征向量"]
    end

    subgraph CNN分支["CNN特征提取分支"]
        D1["SeparableConv1D<br/>深度可分离卷积"]
        D2["Channel Attention<br/>通道注意力机制"]
        D3["特征图输出"]
    end

    subgraph 时序分支["GRU时序建模分支"]
        E1["GRU Layer 1<br/>捕捉长时依赖"]
        E2["GRU Layer 2<br/>简化门耦合设计"]
        E3["时序特征输出"]
    end

    subgraph 融合["特征融合"]
        F1["深度特征 concat 手工特征"]
        F2["全连接融合层"]
    end

    subgraph 压缩["模型压缩"]
        G1["L1正则化"]
        G2["通道剪枝"]
        G3["微调"]
    end

    subgraph 输出["分类输出"]
        H1["Sigmoid输出层"]
        H2["二分类结果"]
        H3["异常概率"]
    end

    subgraph 优化["阈值优化"]
        I["多阈值遍历<br/>寻找最佳平衡点"]
    end

    %% 连接关系
    A1 --> B1
    A2 --> B1
    B1 --> B2
    B2 --> CNN分支
    B2 --> 手工特征
    B2 --> 时序分支

    D3 --> F1
    E3 --> F1
    C --> F1

    F2 --> G1
    G1 --> G2
    G2 --> G3
    G3 --> H1
    H1 --> H2
    H1 --> H3
    H3 --> I

    %% 样式
    classDef input fill:#e1f5fe,stroke:#01579b
    classDef process fill:#fff3e0,stroke:#e65100
    classDef output fill:#e8f5e9,stroke:#2e7d32
    classDef compress fill:#fce4ec,stroke:#c2185b

    class A1,A2 input
    class B1,B2,C process
    class D1,D2,D3,E1,E2,E3 process
    class F1,F2 process
    class G1,G2 compress
    class H1,H2,H3,I output
```

---

## 2. 简化版流程图（论文插图用）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              模型训练流程                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌──────────┐     ┌──────────┐                                            │
│   │FHR信号   │     │UC信号    │                                            │
│   │256×1     │     │256×1     │                                            │
│   └────┬─────┘     └────┬─────┘                                            │
│        │               │                                                    │
│        └───────┬───────┘                                                    │
│                ▼                                                            │
│      ┌─────────────────┐                                                   │
│      │   滑动窗口分割   │                                                   │
│      │  步长64秒/窗口   │                                                   │
│      └────────┬────────┘                                                   │
│               ▼                                                            │
│      ┌─────────────────┐     ┌─────────────────────┐                      │
│      │   归一化处理     │────▶│   手工特征提取        │                      │
│      └────────┬────────┘     │  基线/短期变异/加速/.. │                      │
│               │              └──────────┬──────────┘                      │
│        ┌─────┴─────┐                    │                                   │
│        ▼           ▼                    ▼                                   │
│  ┌───────────┐ ┌───────────┐    ┌──────────────┐                            │
│  │CNN分支    │ │GRU分支     │    │ 5维特征向量   │                            │
│  │Separable │ │双层GRU    │    │              │                            │
│  │Conv1D    │ │长时依赖    │    └──────┬───────┘                            │
│  │Channel   │ │            │           │                                   │
│  │Attention │ │            │           │                                   │
│  └─────┬────┘ └─────┬──────┘           │                                   │
│        │           │                  │                                   │
│        └─────┬─────┘                  │                                   │
│              ▼                        ▼                                    │
│      ┌────────────────────┐ ┌──────────────────┐                          │
│      │   特征融合层        │ │  全连接层         │                          │
│      │  深度特征+手工特征  │ │                  │                          │
│      └─────────┬──────────┘ └──────────┬───────┘                          │
│                │                      │                                   │
│                └──────────┬───────────┘                                   │
│                           ▼                                               │
│                 ┌─────────────────┐                                       │
│                 │ L1正则化+通道剪枝 │                                      │
│                 └────────┬────────┘                                       │
│                          ▼                                                │
│                 ┌─────────────────┐                                       │
│                 │  微调恢复性能     │                                      │
│                 └────────┬────────┘                                       │
│                          ▼                                                │
│                 ┌─────────────────┐                                       │
│                 │ Sigmoid输出层    │                                       │
│                 │ 二分类概率        │                                       │
│                 └────────┬────────┘                                       │
│                          ▼                                                │
│                 ┌─────────────────┐                                       │
│                 │  多阈值遍历优化   │                                      │
│                 │  最佳平衡点       │                                      │
│                 └─────────────────┘                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 各模块详细说明

| 模块 | 具体操作 | 说明 |
|------|---------|------|
| **输入层** | FHR(256×1) + UC(256×1) | 双通道原始信号 |
| **滑动窗口** | 256点/窗口, 步长64 | 64秒数据段 |
| **归一化** | Min-Max归一化 | 0-1范围 |
| **CNN分支** | SeparableConv1D + Channel Attention | 提取局部突变特征 |
| **GRU分支** | 2层GRU (简化门耦合) | 捕捉长时依赖 |
| **手工特征** | 5维: 基线/短期变异/长期变异/加速/减速 | 辅助深度特征 |
| **特征融合** | concat + Dense | 深度特征与手工特征融合 |
| **模型压缩** | L1正则化 + 通道剪枝 | 轻量化 |
| **多阈值优化** | 遍历阈值找最佳F1 | 解决类别不平衡 |

---

## 4. 关键参数

- **采样率**: 4Hz
- **窗口大小**: 256点 (64秒)
- **CNN层**: 深度可分离卷积 + 通道注意力
- **GRU层**: 双层, 隐藏单元数待定
- **手工特征**: 5维
- **正则化**: L1
- **阈值优化**: 遍历[0.1, 0.9]找最佳阈值